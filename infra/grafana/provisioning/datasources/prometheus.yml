# Auto-provisions the Prometheus datasource on `docker-compose up` so the
# latency dashboard works with zero manual Grafana setup (Spec 04 §2 Phase 8).
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
